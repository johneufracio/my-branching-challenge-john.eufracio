version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies...
      - npm install || echo "No packages to install"
  build:
    commands:
      - echo Running build step...
      - npm run build || echo "No build script found"

artifacts:
  files:
    - '**/*'